# Benchmarks

Benchmark items live here once the schema and curation process stabilize.

For now:

- use the schema in `../schemas/benchmark-item.schema.json`,
- start from `examples/example-benchmark-item.yaml`,
- keep provenance and licensing explicit.
