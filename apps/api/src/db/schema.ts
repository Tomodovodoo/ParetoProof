/*
  The first real tables will be added once the database inventory and
  credential-model work is resolved. Drizzle is locked in now, so this file
  exists to make the schema location explicit from the start.
*/

export {};
