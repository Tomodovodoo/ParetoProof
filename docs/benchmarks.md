# Benchmarks

The current benchmark focus is `firstproof/Problem9`.

The repo currently treats it as the canonical implementation slice for:

- benchmark package materialization
- prompt package materialization
- local attempt execution
- verifier output
- offline run-bundle ingest
- hosted worker claim-loop execution

Benchmark work should stay reproducible and versioned. Public pages should only describe released slices, not internal holdouts or private operational evidence.
